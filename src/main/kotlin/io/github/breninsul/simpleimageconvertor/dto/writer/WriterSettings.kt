package io.github.breninsul.simpleimageconvertor.dto.writer

import io.github.breninsul.simpleimageconvertor.dto.settings.Settings

/**
 * The `WriterSettings` interface represents the settings for various file writers.
 * Implementations of this interface provide specific settings for different types of files.
 * It extends the `Settings` interface.
 */
interface WriterSettings: Settings {
}